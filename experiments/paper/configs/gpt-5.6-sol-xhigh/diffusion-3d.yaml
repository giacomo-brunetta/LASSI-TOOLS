# Generated by experiments/paper/generate_configs.py; do not edit.
version: 1
project:
  root: ../../../../../PolyBenchC-4.2.1
kernel:
  name: diffusion-3d
  reference: scientific/diffusion-3d/diffusion-3d.c
  context:
  - scientific/diffusion-3d/diffusion-3d.h
  validation_dataset: large
  task: Translate the cited C kernel into a semantically equivalent, export-friendly PyTorch module. Reproduce
    init_array and operation order from the staged source. Reproduce the explicit seven-point 3D diffusion
    time stepping and return final A. build_inputs(device, dtype, fixture=None, dataset='mini') must accept
    dataset='mini' with TSTEPS=20, N=10 and dataset='large' with TSTEPS=500, N=120. The same make_model()/forward
    implementation must support both profiles. Return live-out A in the exact PolyBench print order. Because
    the paper workflow includes Groq, run lassi-x-compat-wiki targets and select the closest exact Groq/compiler
    snapshot. Inventory the expected aten operators and query that target at fp16 before finalizing the
    module. Do not silently use an A100 or legacy snapshot. Prefer compiled formulations without changing
    reference semantics; a canonical compile result is a preflight signal, not proof of successful model
    compilation.
oracle:
  build:
  - gcc
  - -O0
  - -DLARGE_DATASET
  - -DPOLYBENCH_DUMP_ARRAYS
  - -include
  - ../LASSI-TOOLS/experiments/paper/oracle_precision.h
  - '{reference}'
  - utilities/polybench.c
  - -Iutilities
  - -Iscientific/diffusion-3d
  - -lm
  - -o
  - '{oracle_dir}/reference'
  run:
  - '{oracle_dir}/reference'
  output: oracle.csv
  capture: stderr
  format: polybench
  timeout_s: 600
arena:
  candidates: 3
  planner_format_retries: 2
  correction_rounds: 2
  timeout_s: 900
  equivalence:
    rtol: 1.0e-10
    atol: 1.0e-12
    max_mismatches: 20
models:
  planner:
    provider: custom
    model: gpt56sol
    base_url: http://127.0.0.1:52226/v1
    api_mode: chat_completions
    reasoning_effort: xhigh
    claude_settings: /home/gbrun/.claude/settings.json
    max_tokens: 32768
    max_iterations: 120
    turn_timeout_s: 1200
    turn_retries: 3
  candidates:
  - provider: custom
    model: gpt56sol
    base_url: http://127.0.0.1:52226/v1
    api_mode: chat_completions
    reasoning_effort: xhigh
    claude_settings: /home/gbrun/.claude/settings.json
    max_tokens: 32768
    max_iterations: 120
    turn_timeout_s: 1200
    turn_retries: 3
  - provider: custom
    model: gpt56sol
    base_url: http://127.0.0.1:52226/v1
    api_mode: chat_completions
    reasoning_effort: xhigh
    claude_settings: /home/gbrun/.claude/settings.json
    max_tokens: 32768
    max_iterations: 120
    turn_timeout_s: 1200
    turn_retries: 3
  - provider: custom
    model: gpt56sol
    base_url: http://127.0.0.1:52226/v1
    api_mode: chat_completions
    reasoning_effort: xhigh
    claude_settings: /home/gbrun/.claude/settings.json
    max_tokens: 32768
    max_iterations: 120
    turn_timeout_s: 1200
    turn_retries: 3
  compensation:
    provider: custom
    model: gpt56sol
    base_url: http://127.0.0.1:52226/v1
    api_mode: chat_completions
    reasoning_effort: xhigh
    claude_settings: /home/gbrun/.claude/settings.json
    max_tokens: 32768
    max_iterations: 120
    turn_timeout_s: 1200
    turn_retries: 3
memory:
  enabled: false
execution:
  mode: academy
  exchange_url: https://exchange.academy-agents.org
  auth: globus
  default_resource: harness
  resources:
    harness:
      workspace_root: /home/gbrun/lassi-x-local-academy
      labels:
      - cpu
      - cuda
    a100-node:
      endpoint_id: b162a840-38b4-4c1a-84cc-579fb62f4dfc
      workspace_root: /tmp/lassi-x-a100
      labels:
      - cuda
      - a100
    groq-compute:
      endpoint_id: 428a680f-1efb-488e-9558-96eb7f54a910
      workspace_root: /home/gbrun/lassi-x-groq-academy
      labels:
      - groq-compute
      - lpu
      - direct
  mcp_timeout_s: 1200
  timeout_tolerant_resources:
  - groq-compute
measure:
  precisions:
  - fp64
  - fp32
  - fp16
  - bf16
  strict_precisions:
  - fp64
  - fp32
  evaluation_dataset: large
  warmup: 5
  iterations: 30
  stochastic_seeds:
  - 0
  - 1
  - 2
  - 3
  - 4
  serialize_torch_backends: true
  backends:
  - type: torch
    name: a100-cuda
    device: cuda
    resource: a100-node
    precisions:
    - fp64
    - fp32
    - fp16
    - bf16
    timeout_s: 180
  - type: groq
    name: groq-lpu
    resource: groq-compute
    precisions:
    - fp16
    timeout_s: 1800
    submit_timeout_s: 300
    runtime:
      conda_sh: /home/gbrun/miniconda3/etc/profile.d/conda.sh
      conda_env: groqflow
      python: /home/gbrun/miniconda3/envs/groqflow/bin/python
      pythonpath:
      - /home/gbrun/LASSI-TOOLS/src
    capabilities:
      storage:
      - fp16
      elementwise:
      - fp16
      matmul_accumulator: backend-defined
      explicit_fp32_tensor_ops: false
      fp64: false
compensation:
  enabled: true
  correction_rounds: 2
  error_threshold: 0.01
  error_metric: max_rel_error
  comparison_scope: same_candidate
  measurement_scope: all
runs_dir: ../LASSI-TOOLS/runs/paper-suite/gpt-5.6-sol-xhigh/diffusion-3d
