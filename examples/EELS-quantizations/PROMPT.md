/home/gbrun/soda-benchmarks/models/tensorflow/EELS contains a TensiorFlow implementation of a machine learning model. Propose different optimization techniques (e.g. quantization) and validate their performance. You can run the forward pass by python forward_test.py. check that the result is the same between the original and the refactored. IMPORTANT, for consistency with other tools, velidate the implementation inside the container. Use docker run -it -v /home/gbrun:/home/gbrun  agostini01/soda:latest /bin/bash
